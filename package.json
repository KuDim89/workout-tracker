{
  "name": "workout-tracker",
  "version": "0.1.0",
  "private": true,
  "description": "Workout tracker monorepo",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "dev": "turbo run dev --parallel",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "clean": "turbo run clean --continue"
  },
  "devDependencies": {
    "turbo": "^2.5.0"
  }
}
